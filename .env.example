# Jira CLI environment variables
#
# Usage:
# - Copy to .env and export: `export $(cat .env | xargs)` before running the CLI
#   or use a shell/direnv/dotenv runner that loads .env automatically.
# - Alternatively, configure once using: 
#     jira config set --base-url <url> --email <email> --api-token <token>
#   which saves credentials to ~/.jira-cli.json instead of env vars.

# Base URL of your Jira Cloud site (no trailing slash)
JIRA_BASE_URL=https://jira.srv.westpac.com.au
JIRA_AUTH_MODE=bearer
JIRA_BEARER_TOKEN= #your token here
JIRA_EMAIL= #your email here
JIRA_ACCEPTANCE_FIELD='customfield_10201'
JIRA_DEFAULT_PROJECT=CYMATE
JIRA_ENV_FILE=.env
NODE_TLS_REJECT_UNAUTHORIZED=0